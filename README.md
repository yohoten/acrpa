# A/C RPA - 自动化工作流

轻量级桌面自动化工具，基于 Python + tkinter + pyautogui，通过 Excel 脚本实现图像识别定位、鼠标键盘自动化操作。

- v0.1.14

## ✨ 核心特点

- 可视化 GUI，操作简单  
- Excel 脚本驱动，无需编程  
- **窗口管理**：直接操作Windows窗口，无需图像识别（新增）
- **变量系统**：支持复杂数据处理和数学运算（新增）
- 图像识别定位，精准点击  
- 支持录制回放、脚本编辑器  
- 深色模式、错误重试、日志导出  

## 📋 快速开始

### 环境要求
- Python 3.7+  
- pywin32（窗口管理功能必需）

### 安装与运行

```bash
# 虚拟环境
py -3.7 -m venv .venv
.venv\Scripts\activate          # Windows
pip install -r requirements.txt -i https://pypi.tuna.tsinghua.edu.cn/simple
pip install pywin32             # 窗口管理功能依赖

# 运行
cd ACRPA && python run.py \\ cd src && python ACRPA.py
```

或直接双击 `start.bat`（Windows）。

## 📄 脚本格式

Excel 文件（`.xls`），第1行为标题，第3行开始为命令。

| 列 | 字段 | 说明 |
|----|------|------|
| A | 命令 | 操作类型（找图、按键、等待等） |
| B | 参数1 | 主要参数（图片名、键名、秒数） |
| C~G | 参数2~6 | 辅助参数（精度、坐标、区域等） |

### 常用命令示例

#### 基础命令

| 命令 | 参数示例 | 说明 |
|------|----------|------|
| 找图 | button.png, 0.9 | 全屏查找图片 |
| 点图 | submit.png, 0.9 | 找到并点击 |
| 区域找图 | icon.png, 0.8, 100,100,500,500 | 限定区域查找 |
| 按键 | enter, 1 | 按键（可指定次数） |
| 热键 | ctrl, c | 组合键 |
| 输入 | Hello World | 粘贴输入 |
| 等待 | 2.5 | 延时（秒） |
| 坐标 | 500, 300 | 点击屏幕坐标 |
| 滚轮 | -3 | 滚动行数 |

#### 🆕 窗口管理命令（新增）

| 命令 | 参数示例 | 说明 |
|------|----------|------|
| 激活窗口 | 记事本 | 激活指定窗口（支持模糊匹配） |
| 关闭窗口 | 无标题 - 记事本 | 关闭指定窗口 |
| 最小化窗口 | 计算器 | 最小化窗口到任务栏 |
| 最大化窗口 | 资源管理器 | 最大化窗口 |
| 获取窗口位置 | 记事本,x,y,w,h | 获取窗口坐标和大小 |
| 等待窗口 | Chrome,10,存在 | 等待窗口出现/消失 |

#### 🆕 变量增强命令（新增）

| 命令 | 参数示例 | 说明 |
|------|----------|------|
| 设置变量 | count,100 | 设置变量值 |
| 读取剪贴板 | clip_text | 读取剪贴板到变量 |
| 字符串处理 | text,截取,0:5,result | 字符串操作 |
| 数学运算 | ${x}+${y},sum | 数学计算 |

**变量引用语法**：使用 `${variable_name}` 引用变量

## ⚙️ 配置说明

`config.json` 文件：

```json
{
  "dark_mode": false,      // 深色模式
  "retry_max": 1,          // 最大重试次数
  "retry_interval": 2.0    // 重试间隔(秒)
}
```

## ❓ 常见问题

**找不到图片？**  
- 检查图片路径、识别精度（建议0.8~0.95）、目标窗口是否置前。

**图像识别慢？**  
- **优先使用窗口管理命令**代替图像识别，速度快10-100倍
- 使用区域找图缩小范围、降低精度、减小图片尺寸。

**脚本格式错误？**  
- 必须使用 `.xls` 格式（不支持 `.xlsx`），可用 Excel 另存为。

**录制不准确？**  
- 录制前关闭无关窗口，操作速度平稳，完成后手动修正脚本。

**窗口管理功能不可用？**
- 确保已安装pywin32：`pip install pywin32`
- 以管理员身份运行ACRPA可获得更好效果

## 📚 详细文档

- [窗口管理功能指南](docs/窗口管理功能指南.md)
- [使用说明](res/使用说明.txt)
- [脚本模板](template/脚本模板.xls)
- [测试脚本](template/窗口管理测试.xls)

## 📜 许可证

MIT 许可证。本工具仅限学习测试，请勿用于非法用途。

---

**联系**：yoho12138@aliyun.com  
**更新日志**：
- v0.2.0 (2026-05-25): 新增窗口管理功能和变量增强系统
- v0.1.14: 优化 AI 生成器和模板，提升脚本规范和win命令。
